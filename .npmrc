# Make the "engines" constraint in package.json a hard error rather than a
# warning. Without this npm only prints a notice and installs anyway, which is
# how a too-old Node reaches the build and fails later in a much less obvious
# place -- Pint's Blade rule shells out to Node and dies with ERR_REQUIRE_ESM.
engine-strict=true
